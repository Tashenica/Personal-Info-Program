# Personal Info Program
    - Create a program that:
    - Stores your name, age, and city in variables
    - Uses string interpolation to display them
    - Calculates birth year from age
    - Uses `ToUpper()` to display name in capitals
